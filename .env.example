# === Django ===
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,example.pythonanywhere.com

# === CORS ===
CORS_ALLOW_ALL_ORIGINS=False
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.com

# === Database (PostgreSQL) ===
DB_NAME=bolef_bakkcap
DB_USER=postgres
DB_PASSWORD=change-me
DB_HOST=127.0.0.1
DB_PORT=5432

# === Email (SMTP) ===
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

SMTP_USER=your@gmail.com
SMTP_PASSWORD=your_app_password_16_chars
DEFAULT_FROM_EMAIL=your@gmail.com

# === Google OAuth (optional if you use SocialApp in DB) ===
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-domain.com/auth/google/callback/

# === CSRF (recommended for deploy) ===
# CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://*.pythonanywhere.com
